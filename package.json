{
  "name": "nagya-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Smart newsletter platform for nagya.app — weather-aware, personalized product promos with Resend delivery and PDF coupons.",
  "scripts": {
    "dev": "npm run build && wrangler dev",
    "build": "bash build.sh",
    "deploy": "npm run build && wrangler deploy",
    "kv:create": "wrangler kv namespace create TAGS && wrangler kv namespace create CAMPAIGNS",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.6.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.87.0"
  }
}
